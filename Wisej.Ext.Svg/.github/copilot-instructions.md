# Copilot Instructions

## General Guidelines
- Use qooxdoo/Wisej code style with tab indentation and explicit `function (...)` member syntax (class-style structure similar to qx.Class.define examples).
- For qooxdoo image patches, prefer applying SVG updates via `this.setSource(...)` and avoid manual DOM image manipulation/replacement.